export interface AppSettings {
    layout?: string;
    primaryColor?: string;
    theme?: 'light' | 'dark';
    language?: string; // language code (e.g. 'en', 'es', 'fr')
    direction?: 'ltr' | 'rtl'; // language direction (left-to-right or right-to-left)

}